[package]
name = "magolor-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
logos = "0.13"
# Use the latest inkwell version with LLVM 17 support
inkwell = { version = "0.5", features = ["llvm17-0"] }

[profile.release]
opt-level = 3
lto = true
